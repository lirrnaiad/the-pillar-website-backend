package com.uep.pillar.model.enums;

/**
 * Enum representing the type of media file stored in Cloudinary.
 */
public enum MediaType {
    IMAGE,      // Photos, graphics, illustrations
    VIDEO,      // Video content
    DOCUMENT    // PDFs, documents
}

